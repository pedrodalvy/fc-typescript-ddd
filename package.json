{
  "devDependencies": {
    "typescript": "^5.1.6",
    "gts": "^5.0.0",
    "@types/node": "^20.4.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
